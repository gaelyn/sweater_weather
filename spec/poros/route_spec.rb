require 'rails_helper'

RSpec.describe 'Route' do
  describe 'it encapsulates and returns route info' do
    # it 'returns the route object attributes' do
    #
    #   trip = Route.new(trip_data, forecast)
    #   expect(trip.).to eq()
    #   expect(trip.).to eq()
    # end
  end
end
